apiVersion: v2
name: solarwatch
description: Multi-site solar inverter monitoring platform
type: application
# version = Helm chart version (bump when chart templates change)
# appVersion = app code version (must match VERSION file and Docker image tag)
version: 1.0.0
appVersion: "1.0.0"
keywords:
  - solar
  - monitoring
  - inverter
  - deye
  - sunsynk
maintainers:
  - name: HFI Systems
